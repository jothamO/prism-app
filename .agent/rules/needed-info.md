---
trigger: always_on
---

# Connection Info

VPS details: See local .env.vps file
GitHub username: JothamO

# API Keys

All secrets (Supabase, OpenRouter, Mono, etc.) should be stored in:
- Local `.env` files (not committed)
- VPS `.env` files in each service directory

Never commit secrets to this file.